# EduAI Core — End-to-End User Workflows

> **How to edit this file:** pick a role section below and work it in stages — Claude finds the paths, Claude simulates them via Playwright *and turns that into a committed e2e test* (`tests/e2e/tests/core/`), Claude reviews (its own and another Claude's) work, Claude sweeps once more for gaps, then a human walks the same paths (see the [README](./README.md) for the full methodology). Add/update a row per workflow, including a link to its e2e test — every workflow needs one, it's not optional. Prioritize AI-involving workflows and happy paths first. File bugs as GitHub issues and link them in the Bugs column — don't just describe them in prose. Prefix security findings with `SECURITY:`. Bump **Last updated** every time you edit.

**Last updated:** 2026-08-26 — Claude (Epic #1669: Admin / Unit Admin / Instructor find + simulate + self-review + peer-review + gap-sweep pass; human pass NOT yet done for these three rows)

## Table of contents
- [Human pass — 2026-08-17 (Ahab)](#human-pass--2026-08-17-ahab)
- [Admin](#admin)
- [Unit Admin](#unit-admin)
- [Instructor](#instructor)
- [TA](#ta)
- [Student](#student)

## Human pass — 2026-08-17 (Ahab)

Walked Core as a real user in a browser: typed email/password on `/auth/login` (not cookie injection), clicked through the UI, and checked whether each path made sense. Accounts: `student1@eduai.local` (Alex Patel), `student2@eduai.local` (Brooke Kim), `ta.cs@eduai.local` (Sam Carter). Password is the seed default.

What I actually clicked, and what I saw:

1. **Login / onboarding.** Both Student and TA land on "Link your UBC student number" after sign-in. The skip control is a real button: "I don't have a UBC student number — skip for now". Clicking it as Sam left `/onboarding/student-id` and opened `/dashboard`. Same skip works for Alex. This is the #1464 escape hatch, and it is usable, not just a cookie.
2. **Student dashboard.** After skip: "Good afternoon, Alex", "Your AI-powered learning companion", "Courses enrolled 6", course cards with Chat links. No "Courses assisting". Sidebar chip says "Student". Clear.
3. **TA dashboard.** "Good afternoon, Sam", "Your assigned courses and student activity", **Courses assisting 2**, COSC 101 / COSC 121. Sidebar chip still says "Student" (`ta.cs@eduai.local`) — same design tradeoff already in the table, not a new bug.
4. **Course list.** Student sees "Courses You Are Enrolled In" grouped by term with Enrolled tags (COSC 101, ENGL 110, MATH 200, PHYS 121, …). TA list after a denied redirect is "Courses You Are Assisting In".
5. **COSC 101 (own course).** Instructor card is **Dr. Ada Lovelace**, TA roster is **Sam Carter**. The old "No professor assigned" copy is gone (#1463).
6. **Unrelated course (HIST 210 / MATH 200).** Redirect to `/courses?access=denied` and a red banner: "You do not have access to that course. Open a course from this list only." Makes sense.
7. **Enrolled but unpublished (student2 / COSC 211).** Redirect to `/courses?access=unpublished` and banner: "That course isn't published yet. You'll be able to open it once your instructor publishes it." Makes sense — this is the #1465 fix, and a person can tell it apart from the denied banner.
8. **Course chat (`/chat`, not `/courses/:id/chat`).** History, New chat, composer, and the TA privacy notice ("Instructors, teaching assistants, and platform admins can view this course chat history") all render. I typed a real question and sent it. No assistant reply appeared. That matches #1510 / no reachable model — I am not marking guardrail/off-topic *content* as human-verified.
9. **`/help`.** Student correctly does not see Course materials or Administration. On the tree I walked, TA help also hid Course materials (Getting started / Command palette / Courses / AI chatbot only). That is the defect Stavan flagged. This PR's `help.tsx` Enrollment.role lookup is the fix; the e2e asserts `#materials` visible and `#administration` absent. I am not pretending I re-clicked Help on the post-fix bundle in this same sitting.
10. **`/settings`.** Account / Accessibility / Providers, password-change fields, API keys. Copy is understandable. I did not submit a live password change (would lock the seed account).

I did **not** personally click the API-only probes (question-bank 403s, enrollment IDOR, `canManageTopics` policy toggles, bug-report dialog submit). Those stay on the Playwright assertions added for the review, not on this walk.

Live-model guardrail content is still blocked on a reachable LLM. That remains a known gap, tracked by #1510, not a row I am willing to rubber-stamp.

---

## Admin

Epic #1669 pass (Claude find-paths → simulate via Playwright → self-review → independent peer-review agent → gap-sweep) done on top of the pre-existing course-scope-guardrail coverage below and the already-committed `admin.spec.ts` (user list/promotion, invitation happy path) and `rbac.spec.ts` (admin-only route 401/403 gates). New coverage targets the admin-console surfaces those files don't touch: the policy registry (AI Management/Settings' toggle backend), cross-app bug-report triage, and cross-course chat oversight — plus the UNIT_ADMIN/INSTRUCTOR boundary from the ADMIN side (what those roles can't reach that ADMIN can). Reproducible specs: [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) (API-driven, fresh-user fixtures matching `course-scope-guardrail.spec.ts`'s pattern) and [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) (real-browser walkthrough against the seeded demo accounts, per the epic's "click through the UI" rule). **No human pass has been done on any row in this section** — see the note at the end of this section.

| Workflow | Tester(s) | Status | Makes sense? / UI clear? | Security | Bugs | E2E test |
|---|---|---|---|---|---|---|
| Read a course's off-by-default course-scope guardrail setting | Automated | Automated; human pass pending | API response is explicit and stable | ADMIN may read staff settings | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Enable a course-scope guardrail and verify the persisted read-back | Automated | Automated; human pass pending | Save/read-back path is clear | ADMIN may update staff settings | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| AI Management / policy registry: read policy values + definitions, toggle a flag | Claude (full loop) | Claude pass done — human pass pending | `GET /api/policies` returns `{ policies, definitions }` for ADMIN only (non-admin callers get `policies` with no `definitions`, so the toggle metadata never leaks); `PATCH` persists and reads back immediately. `/admin/settings` renders each flag's `label` text from the same registry the API serves — verified live in a browser, not just via the API. | SECURITY: Verified — non-ADMIN (INSTRUCTOR, UNIT_ADMIN) gets 403 on PATCH; INSTRUCTOR's GET omits `definitions`. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts), [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| Admin console navigation: User Management, AI Management, Bug Reports, Invitations, Settings, Logs, Cron Jobs | Claude (full loop) | Claude pass done — human pass pending | Every `/admin/*` page in the sidebar's Administration group is reachable by clicking the nav link (not just `goto`) and loads its own content without bouncing to `/dashboard` or `/auth/login`. | SECURITY: Verified separately for UNIT_ADMIN/INSTRUCTOR — see their sections' "Administration nav hidden" rows. | None found | [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| Bug-report triage: cross-user list, single-report read, status change | Claude (full loop) | Claude pass done — human pass pending | ADMIN's `/api/admin/bug-reports` lists reports submitted by other users (not just their own — the caller-scoped `mine=true` surface everyone else uses), and `PATCH .../:id` changes status and is reflected immediately on read-back. | SECURITY: Verified — UNIT_ADMIN, despite being a platform-console role, gets 403 on both the list and the PATCH; this triage surface is ADMIN-only, not "any staff role." | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| Cross-course chat oversight (`GET /api/courses/:id/chats`) | Claude (full loop) | Claude pass done — human pass pending | ADMIN's course-chat oversight is unconditional — the shared `courseChatViewPolicyKey` resolver returns the `'always'` sentinel for `admin`, so no policy flag needs to be on (unlike INSTRUCTOR/UNIT_ADMIN's view of the same surface, which are policy-gated and off by default). | SECURITY: Verified — matches the TA section's established "which staff role can read student chats" gate; no policy bypass was needed for ADMIN because none exists to bypass. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| SECURITY: platform-role boundary sweep — what ADMIN can reach that UNIT_ADMIN/INSTRUCTOR cannot | Claude (full loop) | Claude pass done — human pass pending | N/A (boundary check, not a workflow with its own UI). | SECURITY: Verified across every route probed in this pass (`/api/ai-models`, `/api/ai-providers`, `/api/admin/bug-reports`, `PATCH /api/policies`, `/api/users` list) — all 403 for UNIT_ADMIN and INSTRUCTOR. See those roles' sections for the matching rows. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts), [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |

**Not yet covered by this pass** (out of scope for #1669 or environment-blocked, same honesty bar as the TA/Student sections): live AI provider/model CRUD against a real provider (no reachable LLM in this dev environment, same #1510-adjacent gap as chat); System Logs page content beyond "loads without redirecting" (didn't assert specific audit-log rows); Cron Jobs page's actual job-trigger buttons (didn't fire a real cron job, only confirmed the page renders for ADMIN and is blocked for the other two roles). **No human has walked any Admin-section row in this pass** — Ahab's 2026-08-17 human pass (see [Human pass](#human-pass--2026-08-17-ahab)) covered Student/TA only.

## Unit Admin

Epic #1669 pass (Claude find-paths → simulate via Playwright → self-review → independent peer-review agent → gap-sweep). Unit Admin had zero dedicated coverage before this pass (the table below was a single "Not started" placeholder row). Seeded accounts used: `unitadmin.cosc@eduai.local` (`authorizedUnits: ["COSC"]`) and `unitadmin.multi@eduai.local` (`authorizedUnits: ["MATH", "STAT", "DATA"]`) for the UI walkthrough; fresh users promoted to `UNIT_ADMIN` via the E2E-only `/api/e2e/promote` seam (matching `course-scope-guardrail.spec.ts`'s pattern) for the API-driven boundary matrix, since most of what makes UNIT_ADMIN distinct — department-scoped access resolved in `resolveCourseAccessGate` (`apps/core/app/lib/auth/course-access.server.ts`) — is pure RBAC/scope logic verifiable without a dedicated UI of its own. The central finding: **UNIT_ADMIN access is resolved from `Course.department ∈ user.authorizedUnits`, entirely independent of any `Enrollment` row** — a UNIT_ADMIN manages every course in their department whether or not they were ever added to it, which is the core distinction from INSTRUCTOR's enrollment-based single-course ownership. **No human pass has been done on any row in this section.**

| Workflow | Tester(s) | Status | Makes sense? / UI clear? | Security | Bugs | E2E test |
|---|---|---|---|---|---|---|
| Cross-course visibility: see and manage every course in-unit without being enrolled | Claude (full loop) | Claude pass done — human pass pending | `GET /api/courses/:id` (single-course access) and the `/courses` list (via `buildCourseListFilter`'s unit branch) both grant access purely from `department ∈ authorizedUnits` — verified live in the browser for the seeded `unitadmin.cosc` account (sees COSC 101/121/211 with zero `Enrollment` rows) and via a fresh fixture (create a course, add a UNIT_ADMIN with the matching unit, confirm they can read it with no enrollment ever created). Matches the mental model of "unit admin = department oversight," not "unit admin = a list of specific courses." | SECURITY: Verified — cross-course write authority (RAG/guardrail settings, enrollments) works identically to read access: department match is sufficient, no enrollment needed. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts), [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| SECURITY: out-of-unit course is invisible and unmanageable | Claude (full loop) | Claude pass done — human pass pending | A UNIT_ADMIN scoped to `MATH` gets 403 on staff settings (`rag-settings`) and enrollment mutation for a `COSC` course; live in the browser, the seeded COSC unit admin's `/courses` list never shows `MATH 200` and the multi-unit admin's list never shows `COSC 101`. Clean boundary — matches the §19 "unit lock" code comment in `course-access.server.ts` (a `department: null` course is never a match for any UNIT_ADMIN). | SECURITY: Verified — 403 (not 404), so the course's existence isn't hidden, only staff-level access to it; consistent with how other roles' out-of-scope courses behave elsewhere in this doc. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts), [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| SECURITY: cannot create a course outside authorized units, despite `canCreateCourse(role)` being role-level `true` | Claude (full loop) | Claude pass done — human pass pending | `POST /api/courses` succeeds for a department inside the caller's units and returns `403 DEPARTMENT_NOT_AUTHORIZED` for one outside them — the actual gate lives in `createCourse`'s explicit unit check (`apps/core/app/lib/courses/server.ts`), not in `canCreateCourse` from `rbac/permissions.ts`, which only answers "can this role ever create courses" at a coarser grain. Worth knowing precisely which function is the real authority if this code is touched again. | SECURITY: Verified — this is the same rank-vs-scope distinction the doc's Instructor section documents for TA management below; role-level permission and course/department-level scope are two separate checks that both have to pass. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| Manage instructors in-unit (add/reassign an INSTRUCTOR enrollment) | Claude (full loop) | Claude pass done — human pass pending | UNIT_ADMIN (rank 3) meets the rank-3 bar `requiredRankForEnrollmentRole` sets for adding an `INSTRUCTOR` enrollment (`canManageInstructors`: admin/unit only, per `rbac/permissions.ts`) — confirmed live by adding a second instructor to a COSC course as the unit admin. This is the clearest INSTRUCTOR-vs-UNIT_ADMIN boundary in the whole slice: a course's *own, currently-enrolled* INSTRUCTOR (rank 2) cannot add another instructor to their own course — 403 — while UNIT_ADMIN can. | SECURITY: Verified both directions live (unit-admin add succeeds 201; same-course instructor add of a third instructor fails 403). | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| Unit-wide chat oversight (`GET /api/units/:department/chats`) | Claude (full loop) | Claude pass done — human pass pending | Policy-gated (`unitAdmins.canViewUnitChats`, off by default) — unlike ADMIN's unconditional oversight of the same underlying data. Verified the off→403, on→200 transition live with the flag actually toggled via `PATCH /api/policies`, not just read from the registry. | SECURITY: Verified — off by default is the safer posture for reading potentially-sensitive student chat activity across an entire department; toggling requires ADMIN. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| Invitations (`/unit-admin/invitations`), gated by `unitAdmins.canInvite` | Claude (full loop) | Claude pass done — human pass pending | The sidebar's "Invitations" nav item renders even while the policy is off (issue #807's "greyed out, not missing" pattern, confirmed still true here) but the route itself redirects straight to `/dashboard` while the flag is off — verified both the disabled-but-visible nav state AND the redirect live in the browser, then flipped the flag on via `PATCH /api/policies` as ADMIN and confirmed the same UNIT_ADMIN session could now actually reach the page. | SECURITY: Verified — the nav item's visibility and the route's actual access decision are two independent checks that happen to agree here (no case found where the nav promises access the server doesn't grant). | None found | [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| Administration (ADMIN-only) nav group hidden; direct navigation to `/admin/users` redirects | Claude (full loop) | Claude pass done — human pass pending | Confirmed live: the ADMIN_NAV group (`rbac/nav.ts`) never renders for UNIT_ADMIN, and typing `/admin/users` directly in the browser redirects to `/dashboard` rather than rendering a 403 page or leaking any content first. | SECURITY: Verified. | None found | [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| SECURITY: cannot self-elevate `authorizedUnits`, cannot reach the full ADMIN user-management list | Claude (full loop) | Claude pass done — human pass pending | `PATCH /api/users/:selfId` with a wider `authorizedUnits` array is rejected (403) for a UNIT_ADMIN acting on their own account — expanding unit scope is an ADMIN-only action even for the account it targets, mirroring the existing "ADMIN cannot change their own role" self-escalation guard in `admin.spec.ts`. `GET /api/users` (the paginated cross-platform user list) is also 403. | SECURITY: Verified — no self-service unit-scope escalation path exists. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |

**Not yet covered by this pass:** deleting a course in-unit (`unitAdmins.canDeleteCourses` policy — read the flag but didn't exercise a live delete); Canvas integration management from the unit-admin angle (INSTRUCTOR-only surface per the policy registry — confirmed UNIT_ADMIN has no dedicated Canvas UI, but didn't probe the API directly). **No human has walked any Unit Admin row in this pass.**

## Instructor

Epic #1669 pass (Claude find-paths → simulate via Playwright → self-review → independent peer-review agent → gap-sweep) on top of the pre-existing course-scope-guardrail rows below. New coverage focuses on what makes INSTRUCTOR distinct from UNIT_ADMIN/ADMIN (single-course, enrollment-based ownership, several capabilities gated by policy flags that default ON) and from TA (rank-2 vs rank-1 authority — TA's boundaries are already documented in the TA section above; this section doesn't re-test them). Seeded accounts `instructor.cs@eduai.local` (Dr. Ada Lovelace — COSC 101/121) and `instructor.math@eduai.local` (Dr. Emmy Noether — MATH 200) were used for the UI walkthrough; fresh promoted-INSTRUCTOR fixtures for the API-driven policy-flag matrix, since several of these workflows require flipping a policy flag mid-test (state that's cleaner to own on a disposable course than to risk on seed data other rows depend on). **No human pass has been done on any row in this section.**

| Workflow | Tester(s) | Status | Makes sense? / UI clear? | Security | Bugs | E2E test |
|---|---|---|---|---|---|---|
| Read the course-scope guardrail with its default disabled | Automated | Automated; human pass pending | Default is explicit as `false` | Instructor-or-above access is required | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Enable the course-scope guardrail and read it back | Automated | Automated; human pass pending | Toggle persistence is clear | Course ownership is enforced | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Disable an enabled course-scope guardrail | Automated | Automated; human pass pending | On/off transition is reversible | Course ownership is enforced | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Change RAG top-k while preserving an enabled guardrail | Automated | Automated; human pass pending | Independent settings remain independent | No cross-course write observed | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Change RAG relevance threshold while preserving an enabled guardrail | Automated | Automated; human pass pending | Independent settings remain independent | No cross-course write observed | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Save the guardrail and both RAG values together | Automated | Automated; human pass pending | Combined save returns all persisted values | Course ownership is enforced | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Clear RAG overrides while keeping the guardrail enabled | Automated | Automated; human pass pending | `null` restores global RAG defaults | Course ownership is enforced | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Create a course (self-service, `instructors.canCreateCourses` policy, default ON) | Claude (full loop) | Claude pass done — human pass pending | An INSTRUCTOR can `POST /api/courses` for themselves — the create handler auto-force-enrolls the caller as instructor (`forceInstructorUserIds`), so there's no way to create an orphaned course with no owner. | SECURITY: no issue — self-creation only ever names the caller as instructor, never an arbitrary user. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| Manage own-course roster: add a student, create a topic, create a question | Claude (full loop) | Claude pass done — human pass pending | All three actions succeed for the owning instructor via the real API surfaces the Course Manager UI calls; question creation required first creating a topic (the schema's `topicId` is mandatory), which is itself an instructor-level action (`access.rank >= 2` per `courses.topics.$.ts`'s comment referencing §8). | SECURITY: Verified — ownership checked via the shared `resolveCourseAccessGate`, no cross-course leak. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| Own course page in the browser: roster, TA list, materials all render | Claude (full loop) | Claude pass done — human pass pending | Opening COSC 101 as Dr. Ada Lovelace shows Sam Carter as TA (same seed-graph fact the TA/Student sections above already exercise from the other side) and does not redirect to `/courses?access=denied`. | SECURITY: Verified. | None found | [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| SECURITY: zero access to a course owned by a different instructor | Claude (full loop) | Claude pass done — human pass pending | A second instructor gets 403/404 on the course itself, its roster, its staff settings, and its question bank — confirmed as a full surface sweep, not just one endpoint, so there's no partial-leak seam (e.g. settings blocked but roster visible). Live in the browser: MATH 200 (Dr. Emmy Noether's course) never appears in Dr. Ada Lovelace's `/courses` list at all. | SECURITY: Verified — clean boundary, no data leak on any of the four surfaces probed. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts), [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |
| TA/enrollment management, gated by `instructors.canManageEnrollments` (default ON) | Claude (full loop) | Claude pass done — human pass pending | With the default flag on, the owning instructor adds a TA successfully; toggling the flag off as ADMIN and retrying with a second candidate TA is rejected 403 — proves the gate is live-enforced per request, not just read once at course-creation time. | SECURITY: Verified — matches `manageEnrollmentsPolicyKey`'s documented contract (`resolvePolicyGate`/`rbac/permissions.ts`): ADMIN/UNIT_ADMIN are `'always'`, INSTRUCTOR is flag-gated, TA/STUDENT are `'never'`. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| SECURITY: cannot add another INSTRUCTOR to own course (rank boundary vs. UNIT_ADMIN) | Claude (full loop) | Claude pass done — human pass pending | See the matching Unit Admin row above — a same-course INSTRUCTOR (rank 2) is rejected (403) when trying to add a third instructor, because `requiredRankForEnrollmentRole("INSTRUCTOR")` is rank 3. This is the cleanest INSTRUCTOR-vs-UNIT_ADMIN capability boundary found in this pass. | SECURITY: Verified. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| Course-chat oversight (`instructors.canViewCourseChats`, default OFF) | Claude (full loop) | Claude pass done — human pass pending | Off by default — the *opposite* default from `canManageEnrollments` above, and intentionally so: reading a student's chat content is a materially bigger privacy step than adding them to a roster. Verified the off→403, on→200 transition live for the owning instructor's own course. | SECURITY: Verified — matches the TA section's "TA can never view course chats, permanently" finding for the next tier down; INSTRUCTOR's version is merely off-by-default (an ADMIN can turn it on), not hardcoded 'never'. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts) |
| SECURITY: no access to ADMIN or UNIT_ADMIN-only surfaces | Claude (full loop) | Claude pass done — human pass pending | `/api/users`, `/api/invitations`, `/api/ai-models`, `/api/admin/bug-reports` all 403 for a plain INSTRUCTOR; the Administration sidebar group never renders and `/admin/ai-models` redirects to `/dashboard` on direct navigation. | SECURITY: Verified. | None found | [`core-admin-unitadmin-instructor-boundaries.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-boundaries.spec.ts), [`core-admin-unitadmin-instructor-ui.spec.ts`](../../tests/e2e/tests/core/core-admin-unitadmin-instructor-ui.spec.ts) |

**Not yet covered by this pass:** Canvas sync connect/token flow (`instructors.canManageCanvasIntegration`, default ON — read the flag, didn't exercise a live Canvas OAuth handshake, which needs an external Canvas sandbox this environment doesn't have); course delete/publish-toggle UI clicks (the API path for publish is already covered by `course-scope-guardrail.spec.ts`'s fixture helper and `courses.spec.ts`; didn't re-click it here); material upload/rename/delete as INSTRUCTOR specifically (the TA section's AUTH-08 own-upload rule already covers the underlying permission function for TA, and INSTRUCTOR's unconditional any-material access follows directly from `canDeleteMaterial`/`canRenameMaterial` granting `admin`/`unit`/`instructor` unconditionally — read the code, didn't click the UI). **No human has walked any Instructor row added in this pass** (the pre-existing course-scope-guardrail rows above were already marked "human pass pending" before this work started).

## TA

Full-loop pass (find paths -> simulate via Playwright -> self-review -> independent peer-review agent -> gap sweep) done against seeded TA accounts (`ta.cs@eduai.local`, TA in COSC 101/121 only; `ta.math@eduai.local`, TA in MATH 200 only — both have platform `role: STUDENT`, TA status lives entirely in `Enrollment(role=TA)`). Reproducible specs: [`tests/e2e/tests/core/week15-student-ta-exploration.spec.ts`](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts) (round 1) and [`week15-student-ta-exploration-round2.spec.ts`](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) (round 2 — follow-up pass covering the round 1 "known gaps" below plus a fresh sweep, per PR review). Round 2 is 20/20 passing; full `tests/core` suite (both files + everything else, 109 tests) passes with no regressions. Human browser walk done 2026-08-17 for the login/onboarding, dashboard, course, access-denied, chat UI, help, and settings rows (see [Human pass](#human-pass--2026-08-17-ahab)). API-only probes stay on e2e. Live-model guardrail content is still open.

| Workflow | Tester(s) | Status | Makes sense? / UI clear? | Security | Bugs | E2E test |
|---|---|---|---|---|---|---|
| Attempt to open staff-only course-scope settings as a TA | Automated | Automated; human pass pending | Settings should not be offered | TA receives 403 for staff settings | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| First login -> dashboard | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | **Bug found + fixed.** Default post-login landing (`/dashboard`) hit a mandatory "link your UBC student number" onboarding gate with no way to skip, even though the backend already supported skipping. Seeded TAs have no student number and were stuck. See Bugs column. | SECURITY: no issue — gate is auth-scoped correctly, just missing a UI escape hatch. | [#1464](https://github.com/EduAI-Lab/EduAI/issues/1464) — fixed in this branch (added visible "skip for now" button wired to the pre-existing server-side skip action). | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Dashboard content once past onboarding | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | Correct — dashboard's `isTA` detection (any active TA enrollment) renders TA-specific stats/framing ("Courses assisting", not "Assigned courses"), not the generic Student dashboard. | SECURITY: no issue. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Global identity badge (sidebar/profile chip) | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | Shows "Student" for a TA everywhere except the dashboard's own TA-aware content, because `/api/me`'s `role` is literally `STUDENT` (TA status is enrollment-scoped, by design per an inline code comment). Course-specific TA badges elsewhere (e.g. "Courses You Are Assisting In" cards) correctly say "TA". Confusing at a glance but not incorrect — flagging as a design-tradeoff worth a product opinion, not filing as a bug. | SECURITY: no issue — this is a display label, not a permission check; permission checks correctly use `Enrollment.role`. | None filed — deliberate design choice per code comment in `onboarding.server.ts`. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Open own TA course (COSC 101) | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | **Bug found + fixed.** "Instructor" card showed "No professor assigned" despite Dr. Ada Lovelace being the active instructor — seed-data bug (`Course.instructorId` never written), not an access-control issue. TA roster ("Teaching assistants: Sam Carter") rendered correctly throughout. | SECURITY: no issue. | [#1463](https://github.com/EduAI-Lab/EduAI/issues/1463) — fixed in this branch (`prisma/seed.ts` now sets `instructorId` on the course upsert). | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Open an unrelated course (MATH 200, zero relation) | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | Clean, clear UX — redirects to `/courses?access=denied` with a red banner: "You do not have access to that course. Open a course from this list only." | SECURITY: Verified — 403/redirect, no data leaked. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Course chat (AI, own course) | Claude (full loop); Ahab (browser 2026-08-17) | Human walk of the composer/history done 2026-08-17; **live-model reply/guardrail content still pending** | Sent a real message via the browser composer; Auto-routing resolved to a local `vllm:qwen2.5-7b-instruct` tier that is unreachable in this dev environment (no vLLM server running), so no assistant reply arrived and the UI showed no error/timeout indicator within the ~15s test window. Could not verify the course-scope guardrail's off-topic handling as a result — this is a test-environment gap (no local vLLM), not a confirmed product bug, but the complete silence (vs. any loading/error/retry state) is worth a second look in an environment with a reachable model. **Round 2 addendum:** with `/api/chat` mocked to return a deterministic reply (bypassing the unreachable model), the full UI path — composer, send, streamed markdown render — works correctly for a TA in a course-scoped chat. This isolates the round-1 finding: the *UI* isn't the source of the silence, it's specifically the unreachable model backend in this dev environment. Guardrail/off-topic *content* still needs a real reachable model to verify. **Round 3 addendum:** the "silence" is now confirmed, not environment-confounded — see the granular chatbot rows in the Student section below (round 3 tests both roles' composer identically; role only gates access, not UI mechanics), and [#1510](https://github.com/EduAI-Lab/EduAI/issues/1510). | SECURITY: N/A — did not reach a response to evaluate. | [#1510](https://github.com/EduAI-Lab/EduAI/issues/1510) — filed, not fixed (needs a product decision on UX treatment). | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| View questions bank (own course, w/ answers) | Claude (full loop) | Claude pass done — human pass pending | Makes sense — TA sees the full question bank including answers (`GET /api/questions?courseId=...` -> 200, answers present), matching the "TA can grade/prep" use case. | SECURITY: Verified — no issue. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| View questions bank (unrelated course) | Claude (full loop) | Claude pass done — human pass pending | Correctly blocked. | SECURITY: Verified — 403, no leak. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Create a question | Claude (full loop) | Claude pass done — human pass pending | Correctly blocked (instructor+ only). | SECURITY: Verified — 403. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| View own-course roster (enrollments) | Claude (full loop) | Claude pass done — human pass pending | Works as documented — TA(C) sees the full peer roster for their own course. | SECURITY: Verified — 403 for unrelated courses; 200 with real roster only for the TA's own course. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Add a student/TA enrollment | Claude (full loop) | Claude pass done — human pass pending | Correctly blocked — TA is not a policy-granted inviter by default. | SECURITY: Verified — 403. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Upload / rename / delete course materials | Claude (full loop) | Claude pass done — human pass pending | Makes sense — TA can upload freely, and rename/delete only their own uploads (AUTH-08 own-upload rule). Verified live: renaming/deleting the instructor's material -> 403; renaming their own upload -> 200; uploading a new material -> 200/201. | SECURITY: Verified — AUTH-08 null-uploader fail-closed rule holds; no cross-staff tampering possible. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| View course chat oversight (own course's student chats) | Claude (full loop) | Claude pass done — human pass pending | 403 even for the TA's own course. On inspection this is **not** a policy default that's merely off — `courseChatViewPolicyKey()` hardcodes `'never'` for the TA/STUDENT tier; no policy flag can ever grant it. Worth a product question (should TAs get an opt-in oversight view?) but it's a deliberate permanent design decision, not a bug or a misconfigured default. | SECURITY: Verified — correctly, permanently blocked; not policy-configurable. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Admin-only routes (`/api/users`, `/api/ai-models`, `/admin/*`) | Claude (full loop) | Claude pass done — human pass pending | Correctly blocked. | SECURITY: Verified — 403 on every probe. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Mixed role: TA in one course, plain Student in another | Claude (full loop) | Claude pass done — human pass pending | By design (per code comment in `dashboard.tsx`), `isTA` is a single global boolean (any active TA enrollment anywhere), not per-course — so a genuinely mixed-role user sees "TA" dashboard framing site-wide, even for courses where they're purely a Student. Not tested whether this causes any per-course UI to show TA-level controls on a course where the user is only a Student (spot-checked cosc101 for student1-turned-TA-in-hist210 and it correctly kept the Student view for that specific course) — worth a closer human look for edge cases across more course-detail surfaces. | SECURITY: Verified — the mismatched *dashboard framing* does not grant any extra permissions; per-course access checks are independently correct. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Settings page (`/settings`) — account actions | Claude (spot-check round 1; full loop round 2); Ahab (browser 2026-08-17, did not submit a password change) | Human walk of the page done 2026-08-17; live password mutation still e2e-only | The "Student number" card renders with the same generic student-facing copy for a TA ("...so your courses sync automatically") as it does for a real student — consistent with the onboarding-gate finding above, but lower severity since `/settings` doesn't force the field. **Round 2:** password change works correctly end to end (wrong current password -> inline error, no mutation; mismatched confirmation blocks submit client-side before any request is sent; full change-then-relogin verified on a fresh account). Provider API key add/remove round-trips correctly and the raw key is never echoed back to the client (`GET` only ever returns a `hasKey` boolean). The platform-level "Server API Keys" admin surface (`POST /api/auth/api-key/create`) is genuinely server-side gated (403 for both Student and TA), not just hidden in the UI. | SECURITY: Verified — password-change failure paths don't mutate state; provider keys are user-scoped and never leak the raw value; admin API-key creation is blocked server-side for both roles (confirmed by direct API probe, not just UI absence). | None filed separately; same root cause as #1464 for the student-number copy. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Manage course topics (create/rename/delete) — `canManageTopics` policy | Claude (full loop, round 2) | Claude pass done — human pass pending | Policy-gated exactly as documented: with `tas.canManageTopics` OFF (the default), a TA gets 403 on create/rename/delete, even for a topic they'd otherwise own. With it ON, a TA can create and manage *any* topic in the course, including instructor-created/owner-less ones. The more interesting case, confirmed by direct testing rather than just reading the code: a topic a TA created while the flag was ON stays editable/deletable by that same TA even after the flag is turned back OFF — ownership (`createdBy`), not the live policy value, gates the "own-only" carve-out. | SECURITY: Verified — 403 in every gated case; the "own-only survives toggle" behavior is intentional per the route's own code comment, not a bypass. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Chat history: delete, switch between threads, rename | Claude (full loop, round 2) | Claude pass done — human pass pending | Delete and switching between existing threads both work correctly via the UI. **Rename isn't an untested workflow, it's a feature that doesn't exist** — `PATCH /api/chats/:id` isn't a route at all (405 on any method other than GET/DELETE), and there's no rename affordance in the UI either; round 1's framing of this as a "known gap" was slightly off. Delete has no confirmation step (fires immediately on click) — a one-click, irreversible action; not filing as a bug, but worth a product opinion given the earlier "no confirmation" note elsewhere in this doc. | SECURITY: Verified — IDOR-safe. A TA deleting another user's chat by id -> 404; independently confirmed the chat was still present via the real owner's own list (rules out "already gone" as an alternative explanation). | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Submit a bug report via the UI (not just read-back) | Claude (full loop, round 2) | Claude pass done — human pass pending | Full dialog flow works end to end: pick a type, write a description, submit, appears immediately in the caller's own `GET /api/bug-reports?mine=true`. Client-side validation (description under 10 characters) blocks submission with an inline error and keeps the dialog open, matching the server-side minimum. | SECURITY: not specifically re-probed this round — see the Student section's bug-report row for the earlier IDOR-adjacent finding on the read side. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| `/help` page — role-based content | Claude (full loop, round 2); Ahab (browser 2026-08-17, pre-fix tree still hid materials) | Human walk confirmed the gap; fix is in this PR's help loader + e2e | **Fixed in this PR.** TAs can upload/manage their own materials (AUTH-08) but `session.user.role` is `STUDENT`, so the STAFF-only "Course materials" topic was hidden. Help now looks up active `Enrollment(role=TA)` the same way the dashboard does and shows that topic to TAs. Administration stays ADMIN-only. General sections and the "Replay guided tour" link (`/dashboard?tour=1`) are unchanged. | SECURITY: no issue — this is help-content visibility, not a permission change. | Fixed in this branch (help loader `isTA` + HelpView materials carve-out). | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |

**Known gaps for the human pass** (round 2 closed most of round 1's list — see rows above): (1) course chat with a real reachable LLM backend, including the off-topic/course-scope-guardrail *content* — round 2's mocked test confirms the UI rendering path itself works, but the guardrail behavior still needs a live model to verify. Everything else originally listed here (`canManageTopics`, chat delete/rename/switching, bug-report UI submission, `/help`, `/settings` account actions) is now covered — see the round-2 rows above.

**Round 3** (found during round 2's gap-sweep) covered the chatbot-specific items: the model selector, ADHD Assist / Focus mode (via `/api/preferences`), the stop button, and the system prompt dialog — see the granular chatbot rows above, plus the confirmed error-handling bug ([#1510](https://github.com/EduAI-Lab/EduAI/issues/1510)). Still open, not yet tested by any round: (1) `PATCH /api/me` — accepts `name`/`image` with no corresponding UI; worth checking whether it's scoped to the caller only or could target another user's account; (2) logout flow — never verified the session is actually invalidated server-side, not just redirected client-side; (3) the student-ID onboarding form's *link* path (round 1/2 only tested skipping it) — submitting a real/malformed student number and what happens on a Canvas-link failure; (4) command palette (⌘K) — confirm it navigates correctly and doesn't surface admin-only items to a Student/TA. Cosmetic-only surfaces (theme toggle, AI status indicators, materials visibility filter UI) were scanned in round 2's gap-sweep and are low-priority — no UI/data mismatch found worth a dedicated test.

## Student

Full-loop pass done against seeded Student accounts (`student1@eduai.local`, enrolled in 6 published courses; `student2@eduai.local`, enrolled in 7 courses including one **unpublished** one — a useful edge case). Same reproducible specs as the TA section above (round 1 + round 2).

| Workflow | Tester(s) | Status | Makes sense? / UI clear? | Security | Bugs | E2E test |
|---|---|---|---|---|---|---|
| See a published enrolled course while staff settings remain unavailable | Automated | Automated; human pass pending | Course visibility and staff settings are separate | Student receives 403 for staff settings | None found | [`course-scope-guardrail.spec.ts`](../../tests/e2e/tests/core/course-scope-guardrail.spec.ts) |
| Login -> dashboard | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | Clean — "Good evening, Alex", assigned/enrolled courses and recent conversations render correctly, scoped to the student's own enrollments. | SECURITY: Verified — no issue. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Course list (`/courses`) | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | Shows only enrolled, published courses, grouped by term with "Enrolled" tags — clear. | SECURITY: Verified — no cross-account leakage. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Open an enrolled, published course | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | **Bug found + fixed** (same root cause as the TA row above — [#1463](https://github.com/EduAI-Lab/EduAI/issues/1463)): "No professor assigned" shown despite a real instructor. | SECURITY: no issue. | [#1463](https://github.com/EduAI-Lab/EduAI/issues/1463) — fixed in this branch. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Open a course you're enrolled in but that's unpublished (student2/COSC 211) | Claude (full loop); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | **Bug found + fixed.** Direct navigation silently redirected to `/courses` with zero explanation — indistinguishable from "you were never allowed here." Contrast with the clear "you do not have access" banner shown for a genuinely unrelated course. See Bugs column. | SECURITY: Verified — access itself was correctly denied (course never appeared in the student's list, direct GET was blocked); this was purely a UX/clarity gap, not a leak. | [#1465](https://github.com/EduAI-Lab/EduAI/issues/1465) — fixed in this branch (redirect now carries `?access=unpublished` + explanatory banner, matching the existing `access=denied` pattern). | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Course chat (AI, on-topic question in an enrolled course) | Claude (full loop); Ahab (browser 2026-08-17) | Human walk of the composer/history done 2026-08-17; **live-model reply/guardrail content still pending** | Same finding as the TA chat row: Auto-routing resolved to an unreachable local vLLM tier in this dev environment; no reply and no visible error arrived within the test's ~15s window. Not confirmed as a product bug (environment-confounded) — flagged for the human pass with a real provider. **Round 2 addendum:** same as the TA row — with `/api/chat` mocked to a deterministic reply, the full UI path (composer, send, streamed render) works correctly for a Student too. **Round 3 addendum:** see the granular chatbot rows below — the silence is now confirmed across three distinct, deterministic failure modes, not an artifact of this dev environment specifically. | SECURITY: N/A — no response to evaluate. | [#1510](https://github.com/EduAI-Lab/EduAI/issues/1510) — filed, not fixed. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| **Chatbot — model selector** | Claude (full loop, round 3) | Claude pass done — human pass pending | Switching away from the default model correctly changes the `model` field sent to `/api/chat`, and the dropdown trigger reflects the current selection. Real reply content couldn't be verified (no provider in this dev environment has a genuine API key), but the request-construction contract is correct. | SECURITY: N/A — request wiring only. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts), [round 3](../../tests/e2e/tests/core/week15-student-ta-exploration-round3.spec.ts) |
| **Chatbot — ADHD Assist toggle** | Claude (full loop, round 3) | Claude pass done — human pass pending | Toggling "Assist" in the composer correctly sends `adhdAssist: true/false` on the next `/api/chat` request, and persists via `/api/preferences` (`assistDefault`) exactly as the code comment claims — verified by reading it back afterward, not just trusting the comment. | SECURITY: Verified — preference writes are scoped to the caller's own session. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts), [round 3](../../tests/e2e/tests/core/week15-student-ta-exploration-round3.spec.ts) |
| **Chatbot — Focus mode toggle** | Claude (full loop, round 3) | Claude pass done — human pass pending | Confirmed purely client-side (dims non-active content) — toggling it does **not** add anything to the `/api/chat` request body. Matches its stated purpose; not a bug that it's UI-only. | SECURITY: N/A. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts), [round 3](../../tests/e2e/tests/core/week15-student-ta-exploration-round3.spec.ts) |
| **Chatbot — stop button** | Claude (full loop, round 3) | Claude pass done — human pass pending | Clicking "Stop generating" mid-response cleanly returns the composer to its idle state (Send button reappears) with no stray error text, even though the code comments note the AI SDK v4 swallows the resulting `AbortError` (so `onError`/`onFinish` never fire) — the UI doesn't misrepresent an abort as a failure. | SECURITY: N/A. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts), [round 3](../../tests/e2e/tests/core/week15-student-ta-exploration-round3.spec.ts) |
| **Chatbot — system prompt dialog** | Claude (full loop, round 3) | Claude pass done — human pass pending | Saving a custom prompt correctly flows into the next request's `systemPrompt` field; Clear correctly empties it. **Finding:** neither Save nor Clear closes the dialog or shows any confirmation (`api-key-settings.tsx`'s handlers never call `onOpenChange(false)`) — the user gets no feedback that their click did anything. Minor UX gap, not filed separately. | SECURITY: N/A. | None filed separately. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts), [round 3](../../tests/e2e/tests/core/week15-student-ta-exploration-round3.spec.ts) |
| **Chatbot — error handling per `/api/chat` failure mode** | Claude (full loop, round 3) | Claude pass done — human pass pending | **Bug found, not fixed.** Tested three distinct, deterministic failure modes (mocked at the network boundary — a real `502 LLM_STREAM_FAILED`, a real `400` "provider not available", and a connection-refused network error matching a genuinely unreachable model backend). In **all three**, the UI shows nothing: the composer returns to idle with zero error text, toast, or retry affordance. Root cause: `chat-screen.tsx`'s `useChat()` call never destructures the AI SDK's `error` return value, and no component in the chat view tree renders one. This supersedes round 1/2's "environment-confounded, needs a working LLM backend" framing — it isn't environment-specific, it would reproduce identically against a real provider hiccup (rate limit, timeout, auth expiry) in production. The Round 3 spec now **asserts** idle Send/Stop so a green run catches a stuck Stop, and encodes the missing error banner as a `test.fail` expected-failure contract for [#1510](https://github.com/EduAI-Lab/EduAI/issues/1510) (same pattern as #1411/#1412): a real toast fix will unexpectedly pass that check and force the contract to be updated. | SECURITY: no issue — this is a missing UI affordance, not a data exposure. | [#1510](https://github.com/EduAI-Lab/EduAI/issues/1510) — filed, deliberately not fixed (the right UX treatment is a product decision, not something to guess at while testing). | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts), [round 3](../../tests/e2e/tests/core/week15-student-ta-exploration-round3.spec.ts) |
| Cross-course IDOR: chat in a course you're not enrolled in | Claude (full loop) | Claude pass done — human pass pending | N/A (correctly rejected before reaching any UI). | SECURITY: Verified — `POST /api/chat` with an unenrolled `courseId` -> 403 (rejected), no cross-course data returned. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Cross-course IDOR: list enrollments (peers) for an unrelated course | Claude (full loop) | Claude pass done — human pass pending | N/A. | SECURITY: Verified — blocked. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| List enrollments (peers) for your own enrolled course, as a plain Student | Claude (full loop) | Claude pass done — human pass pending | Correctly blocked — matches the "students can't list peers" RBAC rule (only TA+ can). | SECURITY: Verified — 403. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| View the question bank | Claude (full loop) | Claude pass done — human pass pending | Correctly blocked entirely (`GET /api/questions?courseId=...` -> 403) — Students never get direct question-bank access. | SECURITY: Verified — no issue. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| View course materials | Claude (full loop) | Claude pass done — human pass pending | Scoped correctly to the enrolled course; RAG-usable content (lecture notes, course overview) visible as expected. | SECURITY: Verified — a manually-inserted `visibleToStudents: false` "staff only" material was excluded from the student's materials list (TA in the same course correctly still saw it), and the RAG retrieval path (`restrictRagToStudentVisible` in `api/chat.ts` -> `lib/ai/embedding.ts`) threads the same flag, backed by a dedicated `material-visibility.integration.test.ts` covering REST, hybrid-BM25, and pure-vector-SQL paths against one shared oracle — unusually solid coverage here. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Submit / view own bug reports | Claude (full loop) | Claude pass done — human pass pending | `GET /api/bug-reports?mine=true` works and returns only the caller's own reports. | SECURITY: Verified, with a correction — initially read a 404 on `GET /api/bug-reports/<other-user's-id>` as "IDOR blocked"; on inspecting server logs this is actually React Router's own "no route matches" error, since **no** `/api/bug-reports/:id` route exists for any non-admin caller (only `/api/admin/bug-reports/:id` does). So: no leak occurred, but there's also no way for any user — including the report's owner — to fetch a single report by id; only the list view works. Not a security issue, just worth knowing precisely what the 404 means. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Admin-only routes (`/api/users`, etc.) | Claude (full loop) | Claude pass done — human pass pending | Correctly blocked. | SECURITY: Verified — 403. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Chat history: switch between threads, delete via UI | Claude (full loop, round 2) | Claude pass done — human pass pending | Both work as expected. Two chats created, both appear in the sidebar, switching between them by URL and by clicking a row both work; clicking the trash icon deletes the chat (`DELETE /api/chats/:id` -> 204) and it drops out of the list on refresh. No confirmation step before delete — see the equivalent TA row for the same note. | SECURITY: Verified — deleting an already-deleted/nonexistent chat id 404s cleanly (idempotent-safe, no error leak). | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| Submit a bug report via the UI (not just read-back) | Claude (full loop, round 2) | Claude pass done — human pass pending | Client-side validation catches a too-short description (under 10 characters) with an inline error and keeps the dialog open rather than submitting. | SECURITY: not re-probed this round beyond the existing read-side finding above. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| `/settings` — account actions | Claude (full loop, round 2); Ahab (browser 2026-08-17, did not submit a password change) | Human walk of the page done 2026-08-17; live password mutation still e2e-only | Password change: wrong current password shows an inline error without mutating anything (verified by signing back in with the real password afterward); full change-then-relogin verified end to end on a fresh account. Provider API key add/remove round-trips correctly through `hasKey`, raw key never returned to the client; an unknown provider name is rejected with 400. | SECURITY: Verified — same findings as the TA row; password/key handling is correctly scoped to the caller. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |
| `/help` page — role-based content | Claude (full loop, round 2); Ahab (browser 2026-08-17) | Human walk done 2026-08-17 | Same gating as the TA row: "Course materials" (STAFF-only) and "Administration" (ADMIN-only) sections correctly hidden for a Student, general sections render fine, "Replay guided tour" link correctly points at `/dashboard?tour=1`. Nothing role-inappropriate leaks through. | SECURITY: Verified — no issue; this row doesn't carry the TA-specific finding since a Student genuinely shouldn't see either gated section. | None. | [round 1](../../tests/e2e/tests/core/week15-student-ta-exploration.spec.ts), [round 2](../../tests/e2e/tests/core/week15-student-ta-exploration-round2.spec.ts) |

**Environment note for the human pass:** an "Application Error" (`TypeError: Cannot read properties of null (reading 'useContext')` in `useNonce` / `root.tsx`) surfaced twice during this session, on two unrelated pages (chat, settings). Traced to Vite dev-server dependency re-optimization mid-session (stack trace points at Vite's optimized-deps chunks, not app code) — reproduces only in `npm run dev` under heavy first-time-route-discovery, not expected in a production build. A page refresh resolved it both times. Not filed as a bug; flagging so the human tester isn't alarmed if they hit it too.
