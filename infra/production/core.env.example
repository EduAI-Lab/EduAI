# Secret-free production Core template.
# Install the completed file as /etc/eduai/eduai-core.env; do not commit real values.

NODE_ENV=production
PORT=3000

DATABASE_URL=postgresql://<user>:<password>@127.0.0.1:5432/<database>?schema=public
BETTER_AUTH_SECRET=<generate-a-long-random-secret>
BETTER_AUTH_URL=https://my.eduai.ok.ubc.ca
# Leave unset for Core-only host-scoped cookies. Set to .eduai.ok.ubc.ca only
# after the production extension aliases and shared-auth flow are approved.
# COOKIE_DOMAIN=.eduai.ok.ubc.ca

# Core RAG / embeddings. Embeddings are served by the local CMPS Ollama edge.
EMBEDDING_PROVIDER=local
EMBEDDING_DIMENSION=1024
OLLAMA_BASE_URL=http://cmps01.ok.ubc.ca:8001/ollama
OLLAMA_EMBEDDING_MODEL=mxbai-embed-large

# Canvas credential encryption.
ENCRYPTION_KEY=<64-hex-characters>

# Campus inference. Start with reachable hosts only.
VLLM_BASE_URL=http://cmps01.ok.ubc.ca:8001
VLLM_API_KEY=<cmps01-internal-key>
CMPS01_INTERNAL_KEY=<cmps01-internal-key>
VLLM_FLEET_CHAT_URLS=http://cmps01.ok.ubc.ca:8001
# VLLM_FLEET_HEAVY_URL=http://cmps01.ok.ubc.ca:8001

# Redis/BullMQ. Keep enqueue disabled until a worker is deployed and healthy.
REDIS_URL=redis://127.0.0.1:6379
QUEUE_ENQUEUE_ENABLED=false

# Production request limits and logging.
LOG_LEVEL=info
SESSION_VALIDATE_RATE_LIMIT=300
CHAT_RATE_LIMIT=20
CHAT_RATE_WINDOW_MS=60000

# Optional invitation email transport.
# SMTP_HOST=<institutional-relay>
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=<username>
# SMTP_PASS=<secret>
# EMAIL_FROM=EduAI <no-reply@eduai.ok.ubc.ca>

# Core-only release: extension URLs remain unset until aliases are ready.
# VITE_AI_TUTOR_URL=https://aitutor.eduai.ok.ubc.ca
# VITE_QUESTION_MAKER_URL=https://questionmaker.eduai.ok.ubc.ca
