# LiteLLM — Qwen 7B + 32B AWQ (one model per GPU).
# Used by migrate-tiered.sh. Do NOT combine with gpt-oss-120b on the same host.

model_list:
  - model_name: qwen2.5-7b-instruct
    litellm_params:
      model: openai/qwen2.5-7b-instruct
      api_base: http://127.0.0.1:18001/v1
      api_key: vllm-local

  - model_name: qwen2.5-32b-instruct
    litellm_params:
      model: openai/qwen2.5-32b-instruct
      api_base: http://127.0.0.1:18002/v1
      api_key: vllm-local

general_settings:
  master_key: vllm-local
