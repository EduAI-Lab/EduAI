# Copy to .env on cmps01. Never commit .env.
# Generate each secret independently with: openssl rand -hex 32
VLLM_API_KEY=change-me-vllm
EDUAI_INTERNAL_KEY=change-me-internal

# Space-separated IPs allowed to reach the edge. Keep this to s378 only.
INTERNAL_ALLOW_IPS=206.87.25.229
