NODE_ENV="development"

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Better Auth Config
BETTER_AUTH_SECRET="" # REQUIRED: Generate a strong random secret (e.g., `openssl rand -base64 32`)
BETTER_AUTH_URL="http://localhost:5173" # Base URL of your app

# AI Provider API Keys (server-side, takes priority over user-provided keys)
OPENAI_API_KEY="" # Optional: If set, users don't need to provide their own
GOOGLE_GENERATIVE_AI_API_KEY="" # For Embeddings + Chat (if set, users don't need to provide their own)

OLLAMA_BASE_URL="http://localhost:11434/"