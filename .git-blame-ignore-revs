# Commits that only reformat code. `git blame` skips these, so the author who
# last touched a line stays visible through a formatting sweep.
#
# Enable locally, once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub applies this file automatically.
#
# Only add commits that change formatting and nothing else.

# style: format the repo with oxfmt (#1512)
# First repo-wide `oxfmt --write`, 1482 files, oxfmt pinned at 0.63.0.
4e72e6882f8d53aa9a75a75494c8a4b2d609c9b5
