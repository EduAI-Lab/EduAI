{
  "name": "question-maker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=question-maker-*",
    "lint": "turbo run lint --filter=question-maker-*",
    "test": "turbo run test --filter=question-maker-*"
  }
}