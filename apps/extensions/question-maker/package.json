{
  "name": "question-maker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "oxlint . --ignore-pattern app",
    "lint:fix": "oxlint --fix . --ignore-pattern app",
    "format": "oxfmt . '!app'",
    "format:check": "oxfmt --check . '!app'"
  }
}
