import { describe, expect, it } from "vitest";

describe("Canvas live suite", () => {
  it.skip("requires CANVAS_LIVE_TESTS=1 and the protected runtime setup", () => {
    expect(true).toBe(true);
  });
});
